  - name: habilitar puerto {{ item }} fwd
    ansible.posix.firewalld:
      port: "{{ item }}"
      state: enabled
      permanent: true
      immediate: true
      offline: true
    when: ansible_facts['os_family'] == "RedHat"
