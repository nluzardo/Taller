---
- hosts: centos
  user: sysadmin
  become: yes

  tasks:

  - name: Chequear servidor NFS este instalado en Centos
    when: ansible_facts['os_family'] == "RedHat"
    yum:
      pkg: nfs-utils
      state: installed
...
